export declare function getHeroFigureScale(
  currentMetrics: { width: number; height: number } | null,
  baselineMetrics: { width: number; height: number } | null,
  layoutFrame?: { shellWidth: number; availableHeight: number } | null
): number
